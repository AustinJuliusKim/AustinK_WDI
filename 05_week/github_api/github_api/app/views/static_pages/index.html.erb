<div class="container">
	<div class="jumbotron">
		<h1>Ain't no party like a WDI-HTTParty!</h1>
	</div>
	
	 <div class="row">
		
		 	<section>
		 		<% @students.each do |student| %>
		 		 <div class="col-xs-2">
		 			<a target=_blank href=<%= student["html_url"] %>>
		 			<%= image_tag(student["avatar_url"], class: 'img-circle') %></a>
		 			 </div>
		 		<% end %>
		 	</section>
		
	 </div>
</div>