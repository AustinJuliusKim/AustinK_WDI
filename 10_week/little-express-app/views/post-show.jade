link(rel="stylesheet" href="../css/semantic.css")
script(src="../js/socket.io.js")
h1=post.title
h2=post.body

button(id="say-hello") Say Hello

h1 Comments
form(method="POST" action="/posts/#{post.id}")
	input(type="text" name="title" value="#{post.title}")
	input(type="text" name="body" value="#{post.body}")
	input(type="text" placeholder="comment" name="comment")
	button(type="submit") Leave Comment

ul=post.comments[0].comment
div=post.comments.length
div=post.comments

script(type="text/javascript").
	var socket = io();
	var myButton = document.getElementById('say-hello');

	myButton.addEventListener('click', function(){
		socket.emit('What is up, Doc?', 'Not much, Bugs Bunny');
	});
	socket.on("server-says", function(data){
		console.log(data);
	});
	

